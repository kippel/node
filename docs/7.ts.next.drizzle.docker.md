# typescript + next + drizzle + docker

[docs](../readme.md)


```
$ npx create-next-app@latest
```

# .env

```
DATABASE_URL="postgresql://postgres:example@localhost:5432/postgres"
```

```
$ sudo docker compose up --build -d
$ sudo docker compose stop
```