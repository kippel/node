[project]
name = "18-fastapi-pytest-docker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "pytest>=8.4.1",
]


[tool.pytest.ini_options]
pythonpath = "."